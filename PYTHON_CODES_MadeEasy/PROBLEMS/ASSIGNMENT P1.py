#print characters of string using loops
a = "MADEEASYNEWDELHI"
for i in range(len(a)):
    print(a[i]) # The first print(a[i]) will output each character on a new line:
    print(a[i], end = " ") # The second print(a[i], end=" ") will output the same characters, but on the same line, with spaces between them:
  